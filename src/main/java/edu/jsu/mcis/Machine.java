/** 
 * The Machine object implements a queue of Tasks,
 * given by Jobs.
 */
 
public class Machine {

	/*
	 * I figured Task won't be large enough to warrant
	 * its own file, and it doesn't need to exist outside
	 * Machine at all if we give a method in Machine for 
	 * Job to create one. If it gets bloated or something
	 * else needs to edit it, we can migrate it to its own
	 * file.
	 */
	/**
	 * 
	 */
	private class Task {
		
	}

	

}